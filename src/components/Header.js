import React from "react";

function Header(props) {
      return (
        <h1>
          {/* uncomment the line below to pass the test */}
          hello from the Header! {/* Just uncommented this line to pass test */}
        </h1>
      )
}

export default Header;
